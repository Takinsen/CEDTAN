# Triage Labels

The skills speak in five canonical triage roles. This file maps each role to the label string this repo uses.

The tracker is local markdown, so a "label" is the value of the `Status:` line in the issue file. See `issue-tracker.md`.

| Label in mattpocock/skills | Label in our tracker | Meaning |
| --- | --- | --- |
| `needs-triage` | `needs-triage` | Maintainer needs to evaluate this issue |
| `needs-info` | `needs-info` | Waiting on reporter for more information |
| `ready-for-agent` | `ready-for-agent` | Fully specified, ready for an AFK agent |
| `ready-for-human` | `ready-for-human` | Requires human implementation |
| `wontfix` | `wontfix` | Will not be actioned |

When a skill names a role, for example "apply the AFK-ready triage label", use the label string from this table.

Edit the right-hand column to change the vocabulary.
